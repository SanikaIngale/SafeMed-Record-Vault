import React from 'react';
import { View } from 'react-native';

// OTP verification removed — placeholder to avoid build errors for any lingering imports
export default function OTPPlaceholder() {
  return <View />;
}